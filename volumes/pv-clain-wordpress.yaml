apiVersion: v1
kind: PersistentVolumeClaim ## volume usado pelo pod
metadata:
  name: pv-clain-wordpress
  namespace: microcontainers
  labels:
    app: vl-wordpress
spec:
  storageClassName: manual
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi