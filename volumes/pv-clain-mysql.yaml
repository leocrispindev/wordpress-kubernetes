apiVersion: v1
kind: PersistentVolumeClaim ## volume usado pelo pod
metadata:
  name: pv-clain-mysql
  namespace: microcontainers
  labels:
    app: vl-mysql
spec:
  storageClassName: manual
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi